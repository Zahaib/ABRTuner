1. VNCSERVER setting

  sudo apt-get update && sudo apt-get upgrade
  sudo apt-get install ubuntu-desktop gnome-panel gnome-settings-daemon metacity nautilus gnome-terminal
  sudo apt-get install xfce4
  sudo apt-get install vnc4server


[Set security group properly for inbound traffic]


xstartup

//////////////////////////////////////////////////////

#!/bin/sh


export XKL_XMODMAP_DISABLE=1
unset SESSION_MANAGER
unset DBUS_SESSION_BUS_ADDRESS

[ -x /etc/vnc/xstartup ] && exec /etc/vnc/xstartup
[ -r $HOME/.Xresources ] && xrdb $HOME/.Xresources
xsetroot -solid grey
vncconfig -iconic &

gnome-panel &
gnome-settings-daemon &
metacity &
nautilus &
gnome-terminal &

//////////////////////////////////////////////////////


2. Apache2 setting

  sudo apt-get install apache2
  main html location : /var/www/html/


3. Chrome setting

Just go internet and install lastest chrome

wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
sudo dpkg -i --force-depends google-chrome-stable_current_amd64.deb

sudo apt-get install -f


4. Dash compile requirement

 later

 

5. install node v6.11.3 and nodejs v4.2.6

  node
    sudo apt-get install build-essential libssl-dev
    curl -sL https://raw.githubusercontent.com/creationix/nvm/v0.31.0/install.sh -o install_nvm.sh
    bash install_nvm.sh
    source ~/.profile
    nvm install 6.11.3
    nvm use 6.11.3

  nodejs
    sudo apt-get install nodejs

if needed, sudo apt-get -f install

6. Chrome remote interface
  sudo apt-get install npm
  sudo npm install chrome-remote-interface


sudo npm cache clean -f
sudo npm install -g n
sudo n stable

7. ABRSim dependencies:
  sudo apt-get install python-pip python-numpy python-scipy
  sudo pip install statistics
  
  Inside bayesian_changepoint_detection-master
  sudo python setup.py install
   
